# VPA Analysis

The idea is to create a tool that suggests resource limits and requests for all deployments in a namespace or cluster.


Right now, this just looks at all the deployments and creates a VPA for each one.
