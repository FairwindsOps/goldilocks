---
apiVersion: v1
kind: Service
metadata:
  name: goldilocks-web
  labels:
    app.kubernetes.io/name: goldilocks
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: goldilocks
    app.kubernetes.io/component: web
